# PRD：Kimi 运营内容智能助手（Content Copilot）

| 字段 | 内容 |
|---|---|
| 文档版本 | v0.2（Draft，待评审） |
| 撰写日期 | 2026-09-10 |
| 负责团队 | Kimi Operation Team |
| 产品形态 | 内部 Chatbot（Web，对话式工作台） |
| 平台范围 | X、小红书（XHS） |
| 评审状态 | 待 Stakeholder Review |

**v0.2 变更**：① 平台范围去掉 LinkedIn，收敛为 X + 小红书；② 品牌语气规范（`KIMI_VOICE.md` / `KIMI_VOICE_XHS.md`）为既有独立文档，本 PRD 仅做系统级引用，不复述其内容，不定义文案写法；③ 新增 Feature 2 的前置依赖：两份 voice 文件均未覆盖评论区回复语气，需新增回复语气规范。

---

## 1. 背景 & 问题定义（Why First）

### 1.1 现状与痛点

Kimi 运营团队同时维护 **X（Twitter）与小红书（XHS）** 两个平台的自媒体账号。两个平台各有独立的品牌语气规范（`KIMI_VOICE.md` / `KIMI_VOICE_XHS.md`，维护于 kimi-radar 项目），当前工作流存在两类核心成本：

**痛点一：Caption 撰写效率低、语气漂移**
- 两套规范各自包含模板体系、格式规则与 reviewer checklist，约束严格且差异大。运营需要在两套规范间反复切换，容易「串味」，且每篇内容需人工逐项核对 checklist，费时且依赖个人对规范的记忆与执行。
- 一条素材往往要产出两个平台版本 × 多个候选，撰写 + 打磨平均耗时高。

**痛点二：评论处理量大、筛选与回复双重负担**
- 评论量随账号增长线性上升，其中大量是无价值/重复/水军评论；真正值得回复的（提问、高质量反馈、潜在危机）被淹没。
- 「回复哪条、怎么回」目前靠运营同学逐条判断，存在三类风险：
  1. **漏回**：高价值提问被漏掉，用户流失；
  2. **错回**：对杠精/引战评论认真回复，反被截图放大；
  3. **风格失控**：回复语气与品牌声音不一致。

### 1.2 机会

用一个内部 Chatbot 承载这两个高频场景，把品牌语气规范从「文档」变成「系统能力」：规范文件由系统加载为生成约束，文件内的 reviewer checklist 由系统自动执行——语气与合规不再依赖人的记忆。两份 voice 文件本身即声明「Loaded as a constraint whenever a post is generated」，本产品是该声明的工程化落地。

### 1.3 业务目标（可衡量）

| 目标 | 指标 | 基线 | 目标（上线 3 个月） |
|---|---|---|---|
| 提升内容产出效率 | 单条双平台 Caption 的端到端耗时 | 上线前人工基线（需先测量） | 下降 ≥ 50% |
| 保证品牌一致性 | checklist 违规率（自动检查 + 抽检） | 当前人工抽检违规率（需先测量） | < 2% |
| 提升评论运营质量 | 高价值评论回复覆盖率 | 当前人工覆盖率（需先测量） | ≥ 90% |
| 生成质量被认可 | 生成内容采纳率（含轻编辑） | — | ≥ 70% |

> ⚠️ 上线前需先跑 2 周「人工基线测量期」，采集现状数据，否则目标无法归因。

---

## 2. 用户与使用场景

### 2.1 目标用户

- **主要用户**：Kimi 运营团队成员（内容编辑、社区运营），3–10 人规模。
- **次要用户**：运营负责人（关注效率与质量指标，偶尔抽检）。

这是一个**内部工具**，用户量小但使用频率高（每天多次），核心诉求是「快、稳、像我们自己」。

### 2.2 用户故事

**Caption 生成：**
- US-1：作为内容运营，我希望**粘贴素材（链接/图片/要点）并选择平台**，就能得到符合对应平台 voice 文件的多个 caption 候选，以便省去从零撰写与人工核对 checklist 的时间。
- US-2：作为内容运营，我希望对候选**用自然语言继续修改**（「再正式一点」「压缩一下」），以便快速逼近可发布版本。
- US-3：作为内容运营，我希望**一次生成双平台版本**并排对比，以便保证同一素材跨平台信息一致但语气各异。

**评论回复：**
- US-4：作为社区运营，我希望**导入一批评论后自动获得分类与「建议回复/不建议回复」判断**，以便把精力集中在高价值评论上。
- US-5：作为社区运营，我希望每条建议都**附带理由**（为什么值得回/为什么建议不回），以便我快速决策而不是盲目采纳。
- US-6：作为社区运营，我希望对**风险类评论（负面情绪、疑似公关危机）收到升级提醒**，以便及时上报而非机械回复。

---

## 3. 功能需求（What）

> 品牌语气的具体内容（模板、用词、格式、checklist 条目等）以 `kimi-radar/KIMI_VOICE.md`（X）与 `kimi-radar/KIMI_VOICE_XHS.md`（小红书）为唯一事实来源，由系统加载执行；本 PRD 不复述、不重新定义。

### 3.1 Feature 1：Caption 生成（X / XHS）

#### 3.1.1 输入
- **素材输入**（chat 内自然输入，支持组合）：
  - 文本要点 / 草稿
  - 链接（自动抓取正文摘要）
  - 图片（视觉理解，提取可用信息）
  - 活动/发布信息（版本更新、活动 announcement 等）
- **平台选择**：X / XHS / 双平台
- **Post 类型 / Register 确认**（处理按钮，见 §4）：
  - 系统依据素材与 voice 文件的模板体系**自动建议** post 类型/register，用户可通过 chips 确认或切换——类型先于文案，选错类型是最大的返工来源。
- **可选参数**：长度倾向、目的侧重（引流 / 声量 / 产品教育 / 活动）、是否附图片规划提示。

#### 3.1.2 生成逻辑
- 系统按平台挂载对应 voice 文件作为生成约束：X → `KIMI_VOICE.md`；XHS → `KIMI_VOICE_XHS.md`。
- 每平台生成 **2–3 个基于不同模板/不同切入的候选**，而非同质化改写。
- **事实约束**：caption 中的产品功能、数据、日期必须来自用户输入素材，禁止模型补全事实（防幻觉）。无法确认的信息以占位符 `[待确认：xxx]` 标出。
- **自动自检**：生成后自动执行该平台 voice 文件中的 reviewer checklist——可机读项（格式、命名、数字、结构等）自动判定并逐项展示通过/未通过；不可机读项（如「nothing claimed that is not in the brief」的人工确认部分）标记为待复核。这与文件中「auto-flagged by the generator; reviewer confirms」的预期一致。

#### 3.1.3 输出
| 平台 | 输出 |
|---|---|
| X | 依据 `KIMI_VOICE.md` 模板体系的完整 post 候选（含该文件要求的全部组成部分）+ checklist 自检结果 |
| XHS | 依据 `KIMI_VOICE_XHS.md` 的完整 note 候选（标题 + 正文 + tags，含该文件要求的全部组成部分）+ checklist 自检结果 |

- 每个候选卡片展示：checklist 通过率（如 9/10，1 项待人工）、占位符清单。
- 每个候选一键复制；支持「以这条为基础继续改」。

### 3.2 Feature 2：评论回复生成

> ⚠️ **前置依赖**：`KIMI_VOICE.md` 与 `KIMI_VOICE_XHS.md` 均在「Not covered」中明确声明**评论区回复语气未覆盖**。Feature 2 需要**新增《回复语气规范》**（建议命名 `KIMI_VOICE_REPLY.md`，可分平台或统一，由运营基于历史优质回复沉淀），作为 Phase 2 启动条件。在此之前，Feature 2 的回复生成只能以「待规范定稿的临时策略」运行，不作为质量承诺项。

#### 3.2.1 评论输入
一期支持两种方式（见分期）：
- **粘贴导入**（一期）：运营从平台后台复制一批评论粘贴进来。
- **平台拉取**（二期）：对接平台 API 自动同步指定帖子的评论。

#### 3.2.2 评论分类体系（自动）

| 分类 | 定义 | 默认动作 |
|---|---|---|
| Q&A 提问类 | 询问产品功能、价格、使用方式 | **建议回复**（优先级最高） |
| 正面反馈类 | 夸赞、感谢、分享使用体验 | 选择性回复（高赞/有内容的回，纯表情水评不回） |
| 负面反馈类 | 吐槽 bug、体验问题、失望 | **建议回复** + 需人工确认，涉及 bug 转内部 issue |
| 质疑争议类 | 对能力/宣传的真实性质疑、杠精、引战 | 谨慎回复，逐条人工决策 |
| 竞品相关类 | 提及竞品对比、竞品水军 | 默认不回，标记人工 |
| 无关/水评类 | 表情、刷屏、广告、机器人 | 不回复，仅归档 |
| ⚠️ 风险预警类 | 涉及安全、法律、隐私、疑似公关危机苗头 | **禁止自动生成回复**，置顶提示 + 升级通道 |

注：两平台受众与语境差异大（X 偏开发者、XHS 偏大众用户），分类的判定阈值与回复风格按平台区分，分类体系本身共用。

#### 3.2.3 回复选择性策略（Selectivity）

系统对每条评论输出一个 **Reply Score（0–100）**，由以下因子加权（权重一期固定，二期可配置）：

| 因子 | 说明 |
|---|---|
| 回复价值 | 提问可解答性、反馈信息量 |
| 影响力 | 评论点赞数、作者粉丝量（可得时） |
| 时效性 | 是否热点帖、发布时间 |
| 风险系数 | 争议/敏感程度（负向权重） |
| 重复度 | 是否已有同类提问被回复（去重） |

输出分层：
- **S 级（必回）**：Score ≥ 80 或命中风险类 → 置顶展示
- **A 级（建议回）**：60–80
- **B 级（可回）**：40–60，折叠展示
- **C 级（不建议回）**：< 40 或命中无关/水评 → 归档，附一句理由

#### 3.2.4 回复生成
- 每条 S/A 级评论生成 1–2 个回复草稿，语气遵循对应平台的回复语气规范（见前置依赖；规范定稿前遵循临时策略并明确标注「草稿未经规范校验」）。
- 回复草稿附带：分类标签、Reply Score、建议理由（「该用户提问具体且点赞高，建议回复并引导至帮助文档」）。
- **人工确认强制**：所有回复必须由运营点击「确认」后才可复制发布（一期不做自动发布，见 Non-goals）。
- 批量操作：同类 Q&A 支持「用此回复应用到相似评论」。

---

## 4. UI / UX 设计（Chatbot 形态）

### 4.1 形态总述

产品以**对话式工作台**呈现：左侧会话与历史，中间是对话流，能力通过**技能按钮（Skill Buttons）**唤起，参数与操作通过**处理按钮（Process Buttons）**在对话流中内联完成——不做传统表单页，所有交互发生在 chat 里。

### 4.2 核心交互元素

**技能按钮（Skill Buttons）**——输入框上方常驻：
- ✍️ **写 Caption**：进入 caption 生成流
- 💬 **处理评论**：进入评论回复流
- 📚 **语气手册**：查看/检索两份 voice 文件要点（辅助查询，只读）

**处理按钮（Process Buttons）**——跟随对话上下文出现：
- 平台选择 chips：`X` `小红书` `双平台`
- Post 类型/Register chips：系统自动建议 + 可切换（类型列表来源于对应 voice 文件的模板体系）
- 参数 chips：`长度: 短/中/长`、`目的: 引流/声量/教育/活动`、`重新生成`、`以此为基础改`
- 评论卡片上的操作：`✅ 采纳`（复制回复）、`✏️ 编辑后采纳`、`⏭️ 跳过`、`🚩 标记风险`、`应用到相似评论`

### 4.3 关键界面流程

**流程 A：Caption 生成**
1. 用户点击「写 Caption」→ 系统引导输入素材（支持直接粘贴/拖图/丢链接）。
2. 选择平台 chips → 系统**建议 post 类型/register**（chips 形式，可切换）→ 生成 2–3 候选，卡片式并排展示（双平台时按平台分区）。
3. 每个候选卡片：头部显示类型与平台；底部 `复制` `以此为基础改` `重新生成`；卡片上展示 checklist 自检结果（通过项/待人工项/占位符）。
4. 用户自然语言反馈修改（「压缩一下，第二个候选的结尾再软一点」）→ 迭代。

**流程 B：评论处理**
1. 点击「处理评论」→ 粘贴评论（或选择已同步的帖子）。
2. 系统输出**分类概览条**（各分类数量 + S/A/B/C 分布），然后逐条输出评论卡片：
   - 卡片头部：`[分类标签]` + `Score: 78 (A)` + 原评论内容
   - 卡片主体：建议回复草稿（1–2 个）+ 一句建议理由
   - 卡片底部：采纳/编辑/跳过/标记风险
3. 风险预警类评论**不生成回复**，卡片红色描边 + 「建议升级给负责人」按钮。
4. 处理完成后输出本次 session 小结：处理条数、采纳率、跳过原因分布。

### 4.4 设计原则
- **人在回路可见**：任何输出到公开平台的内容，最终一步永远是显式的人工动作（复制/确认）。
- **理由透明**：分类与选择性判断必须给出理由，让运营「校准」而非「盲从」。
- **可逆**：跳过的评论可重新打开；所有生成历史可回溯（会话留存）。

（界面示意见对话内 Mockup。）

---

## 5. 评测方案（Evaluation）

评测分**离线（上线前准入）**、**在线（上线后追踪）**两层，两套体系都先于功能开发启动建设。

### 5.1 离线评测（Gate：不达标不上线）

#### 5.1.1 评测集建设
- **Caption 评测集**：取运营历史发布的 100+ 条真实内容（X / XHS 均衡），由资深运营标注：素材输入 → 理想 caption（人工撰写）。持续追加 bad case 形成 regression set。
- **评论评测集**：取历史评论 500+ 条（两平台），双人独立标注分类 + 应否回复 + 满意回复，计算标注一致性（Cohen's κ ≥ 0.7 才可用作 golden set）。

#### 5.1.2 Caption 生成评测维度

| 维度 | 方法 | 通过标准 |
|---|---|---|
| 规范一致性 | LLM-as-judge + 规则引擎，**直接以对应平台 voice 文件的 checklist 为 rubric** 逐项打分 + 每周 20% 人工复核 judge 一致性 | checklist 平均通过率 ≥ 90%，且平台混淆率（X 味出现在 XHS 版或反之）< 5% |
| 事实准确性 | 与输入素材逐点比对（自动抽取 claim + 人工复核） | 零幻觉事实（幻觉率 = 0 容忍，出现即 block） |
| 结构合规 | checklist 中可机读项（格式/命名/数字）自动判定 | 100% 通过 |
| 可用性 | 盲测：人工撰写 vs 模型生成匿名对比，运营选择「可直接用/小改/重写」 | 「可直接用+小改」≥ 70% |
| 生成速度 | 端到端延迟 | 双平台全量 < 30s |

#### 5.1.3 评论处理评测维度

| 维度 | 方法 | 通过标准 |
|---|---|---|
| 分类准确率 | 对 golden set 计算 per-class Precision / Recall（重点：风险类的 Recall） | 整体准确率 ≥ 85%；**风险类 Recall ≥ 95%**（漏判风险不可接受） |
| 选择性准确率 | 「建议回/不建议回」与标注的 Precision / Recall | 该回未推荐（漏）≤ 10%；不该回被推荐（多）≤ 15% |
| 回复质量 | rubric：与回复语气规范的符合度、准确性、有用性、克制（对杠精不接梗） | 平均 ≥ 4.0 / 5（依赖回复语气规范定稿） |
| 安全性 | 红队用例（诱导生成攻击性/越权承诺的回复） | 0 例违规输出 |

#### 5.1.4 LLM-as-judge 治理
- Judge prompt 与 rubric（即 voice 文件 checklist）版本化管理；每月抽样 50 条人工 vs judge 对比，一致率 < 80% 时修订。
- 评测脚本纳入 CI：voice 文件或 prompt 变更后自动跑 regression set，防止优化 A 场景劣化 B 场景。

### 5.2 在线评测（上线后）

| 指标 | 定义 | 目的 |
|---|---|---|
| 采纳率 | 「直接采纳 / 全部生成」与「采纳（含编辑）/ 全部生成」两个口径 | 生成质量核心指标 |
| 编辑幅度 | 采纳前编辑的字符级修改比例（编辑距离 / 原文长度） | 越小说明越接近「开箱即用」 |
| checklist 修复率 | 自动自检捕获的问题占发布前被修复问题的比例 | 验证「文档→系统能力」的价值 |
| 效率提升 | 使用工具 vs 人工基线的单条内容端到端耗时 | 验证业务目标 1 |
| 评论处理吞吐 | 人均每小时处理评论数 / S 级评论 24h 内覆盖率 | 验证业务目标 3 |
| 互动增益 | 使用 AI 回复的评论 vs 人工回复的评论：被点赞/二次回复率、原评论者后续情绪变化 | 验证回复质量（需 A/B：同帖子内随机 AI/人工） |
| 语气违规率 | 每周负责人抽检 10% 已发布内容的违规占比 | 质量护栏 |
| 升级有效性 | 风险类评论从出现到负责人知晓的中位时长 | 安全护栏 |

### 5.3 评测节奏
- **上线前**：离线 gate 全绿 + 内部 dogfood 2 周（运营真实使用，每日收集 bad case）。
- **上线后**：周报（采纳率、违规率、bad case Top 类别）→ 月度评测集回归 + voice 文件同步（任一 voice 文件更新后需重跑离线评测）。

---

## 6. 指标体系（Metrics）

- **北极星指标**：运营人均有效内容产出（合格 caption 数 + 高价值评论回复数 / 人天）
- **驱动指标**：采纳率、编辑幅度、生成耗时、S 级评论覆盖率
- **健康/护栏指标**：checklist 违规率、事实幻觉数（目标恒为 0）、风险类漏判数（目标恒为 0）、用户周活跃使用天数（内部采用度）

---

## 7. Non-goals（明确不做）

1. **不做自动发布**：一期所有内容人工确认后由运营手动发布或复制到平台，规避平台 ToS 风险与内容事故。
2. **不覆盖 LinkedIn 及其他平台**（LinkedIn、YouTube、TikTok 等）：本期范围仅 X + 小红书，其他平台后续按需求单独立项。
3. **不在本 PRD 定义品牌语气**：voice 的内容与维护属于 `KIMI_VOICE*.md` 文件本身，本产品只做加载、执行与自检。
4. **不做定时排期/内容日历管理**：已有工具或后续单独立项。
5. **不做危机公关自动处理**：风险类只做识别与升级，不生成应对文案（危机话术必须人工主导）。
6. **不做跨平台数据看板**：涨粉/流量分析不在本 PRD 范围。
7. **不替代 voice 规范本身**：`KIMI_VOICE*.md` 仍是唯一事实来源，工具做执行不做定义。

---

## 8. 技术方案要点（供工程评审，非最终决策）

- 架构：内部 Chatbot 前端 + 后端编排服务；`kimi-radar/KIMI_VOICE.md` 与 `kimi-radar/KIMI_VOICE_XHS.md` 作为受控知识注入（system prompt / RAG，版本化，更新可追溯）。两份文件均自述「Loaded as a constraint whenever a post is generated」，注入方式与该预期对齐。
- Caption 链路：素材解析（链接抓取/图片理解）→ 结构化 brief → **类型/register 判定**（依据文件模板体系）→ 按平台模板 + voice 约束生成 → checklist 自动自检 → 输出。
- 评论链路：评论解析 → 分类（prompt 驱动，风险类加规则兜底关键词）→ Reply Score 计算 → 回复生成（分类决定语气策略，遵循回复语气规范）。
- 评测基建：golden set 存储 + 评测脚本 CI 化，与 prompt 版本及 voice 文件版本绑定。

---

## 9. 分期计划

| 阶段 | 范围 | 准出条件 |
|---|---|---|
| **Phase 0（2 周）** | 人工基线测量 + golden set 标注启动 + **《回复语气规范》起草** | 基线数据可用；回复规范初稿完成 |
| **Phase 1（MVP）** | Chatbot 骨架 + Caption 生成（X / XHS，粘贴输入）+ checklist 自检 + 离线评测 gate | 规范一致性/盲测/幻觉指标达标，dogfood 2 周通过 |
| **Phase 2** | 评论粘贴导入 + 分类 + 选择性 + 回复生成（人工确认，**依赖回复语气规范定稿**） | 分类与选择性指标达标，风险类 Recall ≥ 95% |
| **Phase 3** | 评论 API 自动拉取、批量操作、评测 CI 化、指标看板 | 在线指标达到 §1.3 目标 |

---

## 10. 风险与合规

| 风险 | 等级 | 缓解措施 |
|---|---|---|
| 生成不当内容发布损害品牌 | 高 | 人工确认强制 + checklist 自动自检 + 抽检机制 |
| 事实幻觉（编造功能/数据） | 高 | 只允许来自输入素材的事实 + 占位符机制 + 零容忍评测 |
| 风险评论漏判导致公关事件 | 高 | 风险类双通道（模型 + 规则关键词）+ 高 Recall 门槛 + 升级流程 |
| **回复语气规范缺失导致回复质量不可控** | 高 | Phase 2 硬依赖规范定稿；规范前回复草稿明确标注「未经规范校验」 |
| 平台 ToS（自动抓取/发布） | 中 | 一期不自动发布；评论拉取仅用官方 API（XHS 无开放评论 API 时维持粘贴） |
| 内部采用度低（用户不信工具） | 中 | 理由透明 + 快速迭代 bad case + dogfood 期深度共创 |
| `KIMI_VOICE*.md` 更新不同步 | 中 | 文件版本化，更新触发评测回归与系统重新加载 |

---

## 11. 开放问题（待评审决议）

1. **《回复语气规范》**：两份 voice 文件均未覆盖评论区回复语气——分平台还是统一一份？owner 是谁？是否从历史优质回复中提炼？（Feature 2 的关键路径）
2. **voice 文件与系统的同步机制**：`KIMI_VOICE*.md` 更新后如何触发系统重新加载与评测回归（git hook / 手动发布）？
3. 评论数据获取：X 评论 API 权限现状？XHS 无开放评论 API 时的半自动化方案（粘贴增强、剪贴板解析）？
4. 人工基线测量的具体口径（哪些环节计时）需与运营对齐。
5. 双平台一次生成 vs 单平台逐次生成，哪个更符合运营真实习惯（dogfood 验证）。
6. XHS voice 文件提到存在配套的模型侧 profile（`data/voice/xhs_voice_profile.json`），是否纳入评测参照系？
7. 是否需要多人协作能力（如回复草稿的审核流）？一期建议不做，Phase 3 再评估。
